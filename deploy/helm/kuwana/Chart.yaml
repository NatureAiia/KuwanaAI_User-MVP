apiVersion: v2
name: kuwana
description: Kuwana — AI-assisted decision-intelligence platform for Southern Africa (Next.js + Prisma + Supabase)
type: application

# version   — the chart's own version; bump on any template change.
# appVersion — the default image tag, so `helm upgrade` without --set image.tag
#              is still reproducible rather than tracking a moving `latest`.
version: 0.1.0
appVersion: "0.1.0"

kubeVersion: ">=1.27.0-0"

home: https://github.com/NatureAiia/KuwanaAI_User-MVP
sources:
  - https://github.com/NatureAiia/KuwanaAI_User-MVP

maintainers:
  - name: NatureAiia
    url: https://github.com/NatureAiia

annotations:
  category: ApplicationServer
